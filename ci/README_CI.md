# CI/CD (Jenkins) Instructions
1. Configure Jenkins pipeline job (type: Pipeline).
2. Add credentials: GIT_CRED, DOCKER_REGISTRY_CRED, SONARQ_TOKEN, ARGOCD_API_TOKEN.
3. Ensure Docker & Maven are available on the Jenkins agent.
