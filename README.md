# Jenkins End-to-End CI/CD

This repo contains:
- Jenkins declarative pipeline (Jenkinsfile)
- Scripts for Java and Python builds
- Docker build/push logic
- ArgoCD trigger
- Kubernetes manifests

Update the Jenkinsfile values (GIT_URL, DOCKER_REGISTRY, etc.) and connect to Jenkins pipeline.
